# Copy to .env (gitignored) and fill in real values.
# Never commit real keys.

# ----------------------------------------------------------------------
# Brain core
# ----------------------------------------------------------------------
BRAIN_DEFAULT_MODEL=gpt-4o-mini
BRAIN_DEFAULT_LAYER=L1
BRAIN_LOG_LEVEL=INFO

# ----------------------------------------------------------------------
# LLM providers (ADR 0006: V1 = GPT + Claude only)
# ----------------------------------------------------------------------
# Filled when actually wiring real LLM calls (V1 second batch).
OPENAI_API_KEY=
OPENAI_BASE_URL=
ANTHROPIC_API_KEY=

# ----------------------------------------------------------------------
# Langfuse self-hosted observability
# ----------------------------------------------------------------------
# After `docker compose up -d` in infra/langfuse/, register an account
# at http://localhost:3000, create a project, copy keys here.
LANGFUSE_HOST=http://localhost:3000
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=

# ----------------------------------------------------------------------
# Elasticsearch (V2 起用，V1 留空即可)
# ----------------------------------------------------------------------
BRAIN_ES_URL=
BRAIN_ES_USER=
BRAIN_ES_PASS=
